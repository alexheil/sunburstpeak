<%= form_for(@event, url: events_path) do |f| %>

  <div class="hidden">
    <%= f.text_field :start_time, :value => params[:start_time] %>
    <%= f.text_field :end_time, :value => params[:start_time] %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title, class: "text-field" %>
  </div>

  <div class="field">
    <%= f.label :city_id %>
    <%= f.hidden_field :city_id %>
    <%= f.collection_select(:city_id, City.all, :id, :title) %>
  </div>

  <%= f.submit "Create event", data: { disable_with: 'Creating...' }, class: "submit-button" %>
  <% end %>